# Word extraction artifacts (keep .docx in templates/ only)
*_extracted/

# OS & editor
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.cursor/

# Logs & env
*.log
.env
.env.*

# Saved delivery-note archives (generated at runtime)
delivery-notes-archive/*.docx
!delivery-notes-archive/.gitkeep

# Node
node_modules/

# Runtime inventory (persisted on server)
delivery-inventory-data.json
delivery-inventory-data.json.*.tmp

# Nested calculator git metadata backup
toyota-internal-lease-calculator-main/.git.bak-nested/

